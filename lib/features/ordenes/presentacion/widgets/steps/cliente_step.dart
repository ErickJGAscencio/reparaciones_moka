import 'package:flutter/material.dart';

class ClienteStep extends StatelessWidget {
  const ClienteStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Paso 1 - Cliente"),
    );
  }
}