import 'package:flutter/material.dart';

class EquipoStep extends StatelessWidget {
  const EquipoStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Paso 2 - Equipo"),
    );
  }
}