import 'package:flutter/material.dart';

class DetallesStep extends StatelessWidget {
  const DetallesStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Paso 3 - Detalles"),
    );
  }
}